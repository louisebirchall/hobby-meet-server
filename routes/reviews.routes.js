const router = require("express").Router();
const Review = require("../models/Review.model");

// create the main reviews route (list)
// Do we want to show a list with all the reviews??

// create the add reviews route

// create the edit reviews route

// create the delete reviews route

// create the detailed reviews route


module.exports = router;